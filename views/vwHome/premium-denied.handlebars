{{> header}}
  <div class="not-found-area text-center pt-5 mt-5">
            <div class="container">
               
              {{#if user}}
               <h4> You cannot view this premium post. Your account has expired or not register `premium`.<br>
              <strong>Please contact to admin!</strong></h4>
              {{else}}
               <h4> You cannot view this premium post. Your are not login.<br>
                 {{/if}}
                <div class="price-btn common-btn">
                  {{#if user}}
                      <a class="btn-def bnt-2 big" href="/" >Home</a>
                  {{else}}
                      <a class="btn-def bnt-2 big" href="/login" >Login</a>
                  {{/if}}
                   
                </div>
            </div>
</div>
{{> footer}}