{{> header}}

    	<!-- breadcrumbs start -->
		<section class="breadcrumbs-area breadcrumb-opacity bg-fixed">
            <div class="container">
                <div class="row">
                    <div class="col-md-12 text-center">
                        <div class="breadcrumbs">
                            <h2 class="page-title">{{tag.name}}</h2>
                            <ul>
                                <li>
                                    <a class="active" href="/">Home</a>
                                </li>
                                <li>{{tag.name}}</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </section>

      	<div class="container-fluid no-left-padding no-right-padding page-content">
				<!-- Container -->
				<div class="container no-left-padding no-right-padding">
					<div class="row">
						<!-- Content Area -->
						<div class="col-lg-8 col-md-6 content-area">
							<!-- Row -->
							<div class="row blog-masonry-list">

								{{#each posts}}
									{{>single-post post=this haveTags=true haveShortDescription=true}}
								{{/each}} 
                <div class="w-100 text-center">
                  <span> {{isEmpty posts}}</span>
                </div>
							</div><!-- Row /- -->
							<!-- Pagination -->
							<nav class="navigation pagination" >
								<h2 class="screen-reader-text">Posts navigation</h2>
								<div class="nav-links" id="pagination-content">
									
								</div>
							</nav><!-- Pagination /- -->
						</div><!-- Content Area /- -->
						<!-- Widget Area -->
						<div class="col-lg-4 col-md-6 widget-area">
                {{>sidebar posts=mostView tags=tags}}
						</div><!-- Widget Area /- -->
					</div>
				</div><!-- Container /- -->
			</div><!-- Page Content /- -->

{{> footer}}


<script>
    var tag = {{{json tag}}};
    var posts = {{{json posts}}};
    var mostView = {{{json mostView}}};
    var tags = {{{json tags}}};
    var totalPage = "{{totalPage}}"
    var currentPage = "{{currentPage}}"
    var totalPost = "{{totalPost}}"
    var mode_pagination = "{{mode_pagination}}"
    console.log('data',{
      tag,  posts,mostView,tags, totalPage, currentPage, totalPost, mode_pagination 
    });
</script>